{
  "name": "streamable-log-webviewer",
  "version": "0.0.1",
  "description": "Log viewer for displaying logs from syslog in the web",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@seydx/journalctl": "^1.0.0",
    "express": "^4.17.1",
    "simple-syslog-server": "^1.0.0",
    "socket.io": "^4.4.0"
  }
}
